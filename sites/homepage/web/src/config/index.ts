export const guideCodeMap = {
    'step1': `
  ;(async () => {
          const { WebMcpServer, createMessageChannelPairTransport, z, WebMcpClient } = WebMCP
        })()
  `,
    'step2': `
  ;(async () => {
          const { WebMcpServer, createMessageChannelPairTransport, z, WebMcpClient } = WebMCP
        })()
  `,
    'step3': `
  ;(async () => {
          const { WebMcpServer, createMessageChannelPairTransport, z, WebMcpClient } = WebMCP
        })()
  `
  }
  